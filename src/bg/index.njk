---
layout: base.njk
title: DK App Tools | Практични приложения и дигитални инструменти
description: DK App Tools е компактен софтуерен център за практични приложения, продуктови страници, поддръжка, информация за поверителност и полезни статии.
permalink: /bg/index.html
activeNav: home
lang: bg
alternateLabel: EN
alternateUrl: /
---
<section class="home-shell">
  <section class="hero-panel surface-panel">
    <div class="hero-copy">
      <p class="kicker">DK App Tools</p>
      <h1 class="hero-title">
        <span class="hero-title-dk">DK</span>
        <span class="hero-title-main">App Tools</span>
      </h1>
      <p class="hero-lead">
        Практични приложения, ясни продуктови страници, полезни статии и поддръжка на едно място.
      </p>
      <div class="hero-actions">
        <a class="gold-button" href="/bg/apps/">Разгледайте приложенията</a>
        <a class="plain-button" href="/bg/articles/">Прочетете статии</a>
      </div>
    </div>

    <div class="hero-visual" aria-hidden="true">
      <img src="/assets/img/home-hero-showcase.webp" alt="">
    </div>
  </section>

  <section class="compact-panel home-apps-panel">
    <div class="section-head section-head-rich">
      <div>
        <p class="kicker">Избрано приложение</p>
        <h2>Първа версия: Offline FileSecure.</h2>
      </div>
      <a href="/bg/apps/">Разгледайте приложенията</a>
    </div>

    <div class="home-app-feature">
      <article class="app-card live home-featured-product">
        <div class="home-featured-product-copy">
          <span class="home-product-icon" aria-hidden="true">
            <img src="/assets/img/offline-filesecure-icon-clean.png" alt="">
          </span>
          <div>
            <strong>Offline FileSecure</strong>
            <p>Първото приложение от DK App Tools помага да защитавате лични файлове локално на Android, с ясна информация за продукта и поверителността.</p>
          </div>
        </div>

        <div class="home-product-points" aria-label="Offline FileSecure highlights">
          <span>Локален сейф</span>
          <span>Работа офлайн</span>
          <span>Ясна политика за поверителност</span>
        </div>

        <div class="home-app-actions">
          <a class="gold-button" href="/bg/apps/offline-filesecure/">Отворете продуктовата страница</a>
          <a class="plain-button" href="/bg/privacy/offline-filesecure/">Политика за поверителност</a>
        </div>
      </article>

      <article class="home-brand-card">
        <p class="kicker">Бранд център</p>
        <strong>DK App Tools</strong>
        <p>DK App Tools е мястото за нашите приложения, продуктови страници, статии, поддръжка и информация за поверителност.</p>
        <div class="home-brand-links">
          <a href="/bg/support/">Поддръжка</a>
          <a href="/bg/privacy/offline-filesecure/">Поверителност</a>
        </div>
      </article>
    </div>
  </section>

  <section class="compact-panel home-articles-panel">
    <div class="section-head section-head-rich with-link">
      <div>
        <p class="kicker">Последни статии</p>
        <h2>Статии и практични ръководства</h2>
        <p>Статиите засега са достъпни на английски.</p>
      </div>
      <a href="/bg/articles/">Виж всички статии</a>
    </div>
    <div class="article-carousel" data-article-carousel>
      <button class="carousel-button prev" type="button" data-prev aria-label="Previous articles"></button>
      <div class="article-window">
        <div class="article-track">
          {% for article in collections.publishedArticles %}
            {% if loop.index <= 6 %}
              <a class="article-card" href="{{ article.url }}">
                <img src="{{ article.data.cardImage or article.data.image }}" alt="{{ article.data.cardImageAlt or article.data.title }}" loading="lazy">
                <span class="chip">{{ article.data.category }}</span>
                <strong>{{ article.data.title }}</strong>
                <p>{{ article.data.description }}</p>
                <em>Read article</em>
              </a>
            {% endif %}
          {% endfor %}
        </div>
      </div>
      <button class="carousel-button next" type="button" data-next aria-label="Next articles"></button>
    </div>
  </section>

  <section class="compact-panel home-resources-panel">
    <div class="section-head section-head-rich">
      <div>
        <p class="kicker">Стандарт на марката</p>
        <h2>Изградено около практичен софтуер</h2>
        <p>Място за фокусирани приложения, ясни продуктови страници, полезни статии и поддръжка, което расте с реални продукти - не с празни секции.</p>
      </div>
    </div>
    <div class="resource-grid">
      <article class="resource-card">
        <strong>Една ясна задача</strong>
        <p>Всяко приложение трябва да започва с реален проблем и проста причина да съществува.</p>
      </article>
      <article class="resource-card">
        <strong>Без изкуствен мащаб</strong>
        <p>DK App Tools трябва да расте с реални приложения, реални статии и реални страници за поддръжка - не със запълващо съдържание.</p>
      </article>
      <article class="resource-card">
        <strong>Разбираемо преди инсталиране</strong>
        <p>Хората трябва да могат да разберат какво прави едно приложение, какво не прави и къде да получат помощ.</p>
      </article>
      <article class="resource-card">
        <strong>Различни инструменти, един и същ стандарт</strong>
        <p>Бъдещите приложения могат да покриват различни нужди, но стандартът остава същият: ясно, практично и честно.</p>
      </article>
    </div>
  </section>
</section>

<script>
(() => {
  const carousel = document.querySelector("[data-article-carousel]");
  if (!carousel) return;

  const track = carousel.querySelector(".article-track");
  const cards = Array.from(carousel.querySelectorAll(".article-card"));
  const prev = carousel.querySelector("[data-prev]");
  const next = carousel.querySelector("[data-next]");
  const desktopQuery = window.matchMedia("(min-width: 768px)");
  const reducedMotionQuery = window.matchMedia("(prefers-reduced-motion: reduce)");
  const autoplayDelay = 5200;
  let index = 0;
  let autoplayTimer = null;
  let isHoverPaused = false;
  let isFocusPaused = false;

  function update() {
    const card = cards[index];
    if (!card || !track) return;
    const pageY = window.scrollY;
    const nextLeft = card.getBoundingClientRect().left - track.getBoundingClientRect().left + track.scrollLeft;
    track.scrollTo({ left: nextLeft, behavior: "smooth" });
    if (window.scrollY !== pageY) {
      window.scrollTo({ top: pageY, left: window.scrollX, behavior: "auto" });
    }
  }

  function canAutoplay() {
    return cards.length > 3 && desktopQuery.matches && !reducedMotionQuery.matches;
  }

  function stopAutoplay() {
    if (!autoplayTimer) return;
    window.clearInterval(autoplayTimer);
    autoplayTimer = null;
  }

  function startAutoplay() {
    stopAutoplay();
    if (isHoverPaused || isFocusPaused || !canAutoplay()) return;
    autoplayTimer = window.setInterval(() => {
      index = index >= cards.length - 1 ? 0 : index + 1;
      update();
    }, autoplayDelay);
  }

  function restartAutoplay() {
    if (!canAutoplay()) return;
    startAutoplay();
  }

  prev.addEventListener("click", () => {
    index = index <= 0 ? cards.length - 1 : index - 1;
    update();
    restartAutoplay();
  });

  next.addEventListener("click", () => {
    index = index >= cards.length - 1 ? 0 : index + 1;
    update();
    restartAutoplay();
  });

  carousel.addEventListener("mouseenter", () => {
    isHoverPaused = true;
    stopAutoplay();
  });

  carousel.addEventListener("mouseleave", () => {
    isHoverPaused = false;
    startAutoplay();
  });

  carousel.addEventListener("focusin", () => {
    isFocusPaused = true;
    stopAutoplay();
  });

  carousel.addEventListener("focusout", () => {
    isFocusPaused = carousel.contains(document.activeElement);
    if (!isFocusPaused) startAutoplay();
  });

  desktopQuery.addEventListener("change", startAutoplay);
  reducedMotionQuery.addEventListener("change", startAutoplay);
  startAutoplay();
})();
</script>
